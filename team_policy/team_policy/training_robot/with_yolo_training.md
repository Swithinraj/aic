# Moved

The complete YOLO collection, conversion, training, and inference guide now
lives in [README.md](./README.md).

This file is intentionally kept as a short pointer so there is only one
authoritative workflow document in `training_robot/`.
